<!-- TODO: Format all pages (new, edit, show) as well -->
<div class="section">
  <div class="container">
    <h1 class="title is-1">Posts</h1>
    <% @posts.each do |post| %>
      <div class="box">
        <div class="level">
          <div class="level-left">
            <div class="level-item">
              <h1 class="title is-3"><%= post.title %></h1>
            </div>
          </div>
          <div class="level-right">
            <div class="level-item">
              <div class="buttons">
                <%= link_to 'Show', post, class: "button is-link" %>
                <% if current_user.id == post.creator_id %>
                  <%= link_to 'Edit', edit_post_path(post), class: "button is-link" %>
                  <%= link_to 'Delete', post, method: :delete, data: { confirm: "Are you sure you want to delete this post?" }, class: "button is-danger "%>
                <% end %>
              </div>
            </div>
          </div>
        </div>
        <h3 class="subtitle is-5"><%= post.created_at.strftime('%B %d, %Y') %></h3>
        <p class="content">
          <%= post.content %>
        </p>
      </div>
    <% end %>
  </div>
</div>

